/*
const {Activity}= require('../db.js');



it('test_create_activity', async () => {
    const activity = await Activity.create({
        name: 'Test Activity',
        difficulty: '1',
        season: 'verano'
    });
    expect(activity).toBeDefined();
    expect(activity.id).toBeDefined();
    expect(activity.name).toBe('Test Activity');
    expect(activity.difficulty).toBe('1');
    expect(activity.duration).toBeNull();
    expect(activity.season).toBe('verano');
});
*/